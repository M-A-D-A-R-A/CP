#include <bits/stdc++.h>
using namespace std;
int main(){
    int t;
    string s ="";
    int ascii;
    cin>>t;
    while (t--)
    {
        for (int i = 1; i < s.length(); i++)
        {
            s=s+s[i];
        }
        
    }
    
}